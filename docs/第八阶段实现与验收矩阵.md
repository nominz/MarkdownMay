# 第八阶段实现与验收矩阵

> 上位需求：`需求规格说明书_V05.md` 1.2 冻结基线受控修订
>
> 启动基线：`475241d docs: hand off new phase 8 scope`
>
> 状态：8.0～8.6 已完成；8.7 自动验收完成；新增 8.8 正在实施，完成后由 8.9 汇总视觉复核并封闭

| 增量 | 范围 | 主要产物 | 门禁/证据 | 状态 |
| ---: | --- | --- | --- | --- |
| 8.0 | V05、影响分析、总体/详细设计、追溯矩阵 | V05、CR-014～018、DC-2026-006、本矩阵 | 2026-08-12 用户明确批准；本地提交 `8c84cf9` | 已完成 |
| 8.1 | 文档类型与三模式插入点映射 | `DocumentKind`、`SourcePositionMapper` | UTF-8、CRLF、过期修订、既有三模式回归 | 已实现并验证 |
| 8.2 | 双目标切分 | `DocumentSplitService`、双新目标提交协议 | 原文件/会话不变、扩展名与编码继承、竞争补偿 | 已实现并验证 |
| 8.3 | 插入文件事务 | `DocumentCombineService`、资源计划 | 编码/换行、链接、图片、来源不变、单事务提交 | 已实现并验证 |
| 8.3B | Word/PDF 文字插入风险样机 | 依赖、体积、安全和提取质量证据 | 用户据样机决定放弃两类插入，不引入 reader | 已完成并撤销实现范围 |
| 8.4 | TXT 正式文档链路 | 打开/保存/另存/命令门禁/UI | 纯文本往返、编码换行、无误解析 | 已实现并验证 |
| 8.5 | `.md/.txt` Windows 集成 | 两 ProgID、Capabilities、候选注册/撤销/默认应用引导 | 隔离 HKCU、撤销所有权、UserChoice 零写入 | 已实现并验证 |
| 8.6 | 应用/MD/TXT 图标 | RC/ICO/DefaultIcon/Shell 刷新 | ICO 图层、PE 资源 ID、ProgID 索引 | 已实现并自动验证；人工外观留 8.7 |
| 8.7 | 原范围自动回归 | Debug/Release、全量 CTest、报告 | 自动门禁通过；资源管理器视觉待用户复核 | 已完成自动部分 |
| 8.8 | 安置便携程序 | 原生路径对话框、自复制事务、等待旧进程、关联修复 | 临时复制/校验/替换、脏文档、参数安全、原 EXE 保留 | 已实现并自动验证；真实迁移留 8.9 人工复核 |
| 8.9 | 第八阶段汇总验收与封闭 | 8.8 回归、资源管理器视觉检查、交接报告 | Debug/Release 全量通过且用户完成视觉复核 | 未开始 |

## 10. 8.8 验收记录

### 实现范围

- “工具”菜单新增“安置马冬梅…”，原生模态窗口包含可编辑目标文件夹、`IFileDialog(FOS_PICKFOLDERS)`“浏览…”按钮、“安置”和“取消”；默认路径为 `%LOCALAPPDATA%\Programs\MarkdownMay`，正文明确说明程序仅约 2 MB、可放心放在系统盘。
- `ApplicationPlacementService` 固定目标名 `MarkdownMay.exe`，使用目标目录内 GUID 临时文件、独占创建、分块复制、`FlushFileBuffers`、逐字节重读校验和 `MoveFileExW(...WRITE_THROUGH)` 原子提交；不删除原 EXE，不处理目标目录其他文件。
- 目标已存在且内容不同必须确认；内容相同仍重新复制校验并在确认目标未变化后原子替换，避免“检查后、启动前”竞态；确认后目标大小或写入时间改变返回 7404，不覆盖。
- 脏文档复用现有保存/取消门禁。新进程接受严格内部参数 `--wait-for-process <pid> --repair-file-types [document]`，在单实例仲裁前限时等待旧进程退出，随后修复候选关联并恢复当前已保存文档。
- 同一路径不复制、不重启，只按当前路径修复候选文件关联；继续禁止写 `UserChoice`。只有 `CreateProcessW` 成功后旧进程才绕过重复保存提示正常退出；启动失败时旧进程继续运行。
- 新增稳定错误码 7401～7405，并同步《错误码表》；没有新增第三方依赖、安装器、更新器、后台常驻或运行时下载。

### 自动验证证据（2026-08-15）

| 验证 | 结果 |
| --- | --- |
| Debug 受影响目标构建 | `MarkdownMay`、安置、启动参数、应用外壳目标通过 |
| Debug 定向测试 | 3/3 通过 |
| Debug `ALL_BUILD` | 通过 |
| Debug 全量测试 | 一次 41/41 通过；最终复核 40/41，唯一既有 `wysiwyg_input_integration` 瞬时时序失败，单独复跑通过；8.8 测试始终通过 |
| Release `ALL_BUILD` | 通过 |
| Release 全量测试 | 41/41 通过 |
| 最终安全收紧后 Release 定向测试 | 3/3 通过 |
| 工作树检查 | `git diff --check` 通过；用户未跟踪文件未触碰 |

自动测试覆盖新目标、同路径、相同内容替换、不同内容需授权、确认后外部变化、临时文件清理、严格 PID、等待/修复参数组合、菜单和应用外壳回归。为避免自动测试改写用户真实候选关联或启动第二份正式程序，本轮没有自动执行真实“安置”按钮；对话框外观、浏览按钮、真实关闭/重启、关联切换和原 EXE 保留由 8.9 用户侧人工复核。

## 1. 8.0 评审清单

- [x] 切分不进撤销栈，原文件和会话不变。
- [x] 插入是单次可撤销事务，来源文件不变。
- [x] 提出 TXT 纯文本语义和 Markdown 专属功能门禁。
- [x] 澄清 TXT 语义指直接打开/编辑 TXT，不是 Markdown 导出 TXT。
- [x] 明确 `.md/.txt` 插入跨编码静默转为当前文档保存编码。
- [x] Word/PDF 插入列为先样机后实现的 8.3B，扫描 PDF 不含 OCR。
- [x] 区分候选注册与 Windows 最终默认选择，禁止 `UserChoice`。
- [x] 冻结文档图标资源和单 EXE 边界。
- [x] 完成影响分析和设计同步。
- [x] 用户批准 V05 及设计。
- [x] V05 改为 1.0 冻结并记录批准日期。
- [x] 形成 8.0 本地 Git 提交 `8c84cf9`。

## 2. 追溯入口

| 需求组 | 设计 | 预计实现 | 测试 |
| --- | --- | --- | --- |
| SPLIT | DC-2026-006 / CMP-COMB-01～03 | `src/services/document_split.*` | 服务单元、`document_split_*` 集成 |
| INSERT | DC-2026-006 / CMP-COMB-01、04～06 | `src/services/document_combine.*` | 服务单元、`document_insert_*` 集成 |
| TXTDOC | DC-2026-006 / CMP-DOC-07、CMP-FILE-08 | session、file、view/UI | 文档/文件单元、`txt_document_*` |
| ASSOC | DC-2026-006 / CMP-PLAT-01～02 | association、设置 UI | 隔离 HKCU `association_*` |
| ICON | DC-2026-006 / CMP-PLAT-03 | RC、ICO、ProgID 索引 | PE/ICO 结构 + 人工矩阵 |

每个增量先验证再本地提交，不推送；保留现有未跟踪内容。未实际执行的构建、Windows、DPI、主题、性能或人工操作不得标为通过。文档 ICO 未到位时不得封闭 8.6 或第八阶段。

## 6. 8.4 验收记录

### 实现范围

- 打开对话框、拖放、最近文件、命令行/单实例转发最终统一进入 `DocumentWindow::open_document`；按 `.txt` 扩展名建立 `plain_text` 会话，`.md/.markdown` 建立 Markdown 会话，无扩展名不自动当作 TXT。
- `DocumentSession::reload` 可在文件替换时原子更新正文与 `DocumentKind`；TXT 继续使用同一 UTF-8 源码、修订、脏状态和撤销观察链，不创建第二正文模型。
- TXT 打开后固定使用源码式纯文本表面；渲染、对照、大纲、Markdown 格式命令、打印、页面设置和 Markdown 语义导出门禁关闭，查找替换、剪贴板、撤销重做和保存继续可用。
- 打开和保存沿用既有严格 UTF-8/BOM UTF-16、编码保留、主导换行与原子写入。状态栏明确显示“纯文本”、实际编码和换行。
- “另存为”支持 `.txt` 与 `.md/.markdown` 相互转换；转换只更新文档类型，不添加、删除或解释正文标记。
- DOCX/PDF 插入经 CR-2026-019 正式撤销；不引入 `miniz`、PDFium 或其他新 reader 依赖。

### 自动证据

| 场景 | 结果 |
| --- | --- |
| TXT 中 `#`、`*` 不生成 Markdown 语义 | 通过 |
| TXT 固定源码模式；渲染/对照/大纲/格式/打印/语义导出禁用 | 通过 |
| 查找与保存仍可用，状态栏显示“纯文本” | 通过 |
| UTF-8 BOM + CRLF 打开保存往返 | 通过 |
| TXT 另存为 Markdown 只换类型、不改正文 | 通过 |
| 文档类型 reload/change_kind 单元测试 | 通过 |
| Debug 定向应用/文档/UI 目标构建 | 通过 |
| 全量 Debug CTest | 40/40 通过（含隔离 HKCU 和 Win32 UI） |
| Release 正式应用、文档单元和 UI 集成目标构建 | 通过 |

## 7. 8.5 验收记录

### 实现范围

- 当前用户 HKCU 下建立 `MarkdownMay.Document.Markdown` 和 `MarkdownMay.Document.Text` 两个稳定 ProgID；`.md/.markdown` 指向前者，`.txt` 指向后者。
- `RegisteredApplications/Capabilities/FileAssociations` 同时声明 `.md/.markdown/.txt`；打开命令统一为规范 EXE 路径加安全引用的 `"%1"`，不解析额外命令片段。
- 注册只写 `OpenWithProgids` 与 Capabilities，不写扩展名默认值，不创建、删除或修改 `UserChoice`，因此注册成功只表示“候选可选”，不表示已成为默认程序。
- 注册/修复兼容迁移本应用旧 `MarkdownMay.Document` 命令和值；只有旧命令仍匹配当前注册命令时才清除旧叶值。
- 撤销绑定当前规范 EXE 路径，逐项比较 ProgID 描述、图标、命令、Capabilities 后才删除匹配的自有值；被外部改写的命令、第三方 `OpenWithProgids` 和未知值全部保留。
- EXE 移动沿用 `needs_repair` 与按当前路径记忆“暂不修复”；注册成功清除忽略记录。
- 默认应用动作优先打开注册应用的 Windows 设置入口，失败回退 `ms-settings:defaultapps`；界面文案明确说明候选注册不会抢占默认应用。
- 注册、修复和撤销后调用 `SHChangeNotify(SHCNE_ASSOCCHANGED)`；8.6 再接入独立文档图标资源索引。

### 自动证据

| 场景 | 结果 |
| --- | --- |
| 两 ProgID 与 `.md/.markdown/.txt` Capabilities | 通过 |
| 含空格/中文/Emoji EXE 路径安全引用 | 通过 |
| EXE 移动检测、忽略与修复 | 通过 |
| 撤销保留第三方 OpenWith 值和未知 ProgID 值 | 通过 |
| 撤销保留被外部改写的 TXT 打开命令 | 通过 |
| 隔离 HKCU 中不存在 `.md` UserChoice 写入 | 通过 |
| 默认应用注册应用 URI 契约 | 通过 |
| Debug 关联、启动转发定向测试 | 2/2 通过 |
| Debug 正式应用构建 | 通过 |
| 全量 Debug CTest | 40/40 通过（含隔离 HKCU） |
| Release 正式应用与文件关联测试目标构建 | 通过 |

## 8. 8.6 验收记录

### 实现范围

- 接收用户更新后的 `resources/icons/MarkdownMay.ico` 作为应用图标，以及 `resources/icons/MarkdownMay.documents.md.ico` 作为当前文档图标。
- 资源 ID 固定为 101（应用）、102（Markdown 文档）、103（TXT 文档）。Markdown/TXT 当前共同嵌入用户提供的文档 ICO，但 ID 独立，未来 TXT 专用图标可直接替换 103 而不改变 ProgID。
- `MarkdownMay.Document.Markdown/DefaultIcon` 使用带引号的规范 EXE 路径和 `-102` 正资源 ID；TXT 使用 `-103`，不依赖 ICO 伴随文件或不稳定的零基索引。
- 注册、修复、撤销继续调用 `SHChangeNotify(SHCNE_ASSOCCHANGED)`；所有权安全撤销按各自资源引用逐值比较。

### 验证证据

| 场景 | 结果 |
| --- | --- |
| 应用 ICO 图层 | 16/24/32/48/64/128/256，全部 32 位，通过 |
| 文档 ICO 图层 | 16/24/32/48/64/128/256，全部 32 位，通过 |
| Debug EXE RT_GROUP_ICON 101/102/103 | 三项均存在，组资源各 104 字节 |
| 隔离 HKCU Markdown/TXT DefaultIcon | 分别为 `,-102` 与 `,-103`，通过 |
| 全量 Debug CTest | 40/40 通过 |
| Release 正式应用及关联测试目标 | 构建通过 |
| 资源管理器多视图、100%～200% DPI、浅/深/高对比度人工观感 | 未执行，留 8.7 人工验收，不虚报通过 |

## 3. 8.1 验收记录

### 实现范围

- `DocumentSession` 新增 `DocumentKind { markdown, plain_text }`，构造默认仍为 Markdown，既有调用兼容。
- `SessionSnapshot` 固定携带文档类型；纯文本会话保留唯一 UTF-8 源码、修订、脏状态和事务，但不解析或伪造 Markdown 语义树。
- 纯文本会话禁用语义提交、后台 Markdown 解析结果接收和 `can_export()`，为 8.4 UI 能力门禁提供稳定底层契约。
- 新增 `SourcePositionMapper::MapCaret`，统一验证表面/会话修订一致、折叠插入点、源码范围、UTF-8 码点边界和 CRLF 边界。
- 既有 RichEdit `ProjectionMap`、Scintilla UTF-8 选区和对照左栏仍负责原生位置到源偏移换算；后续切分/插入只使用本公共门禁，不建立三套业务映射。

### 自动证据

| 项目 | 结果 |
| --- | --- |
| 定向 `MarkdownMayDocumentUnit` Debug 构建 | 通过 |
| `document_parser_unit` | 通过 |
| 纯文本 `#` 不生成 Markdown 语义、事务仍可提交 | 通过 |
| 过期修订、非折叠选区、UTF-8 中间、CRLF 中间拒绝 | 通过 |
| 完整 Debug 构建（单并发复核） | 通过 |
| 全量 Debug CTest | 38/38 通过；其中注册表测试在可访问隔离 HKCU 的环境复核通过 |
| Release `MarkdownMay` 构建 | 通过 |

第一次完整 Debug 构建超过 180 秒工具时限并遗留 MSBuild/CL 进程；清理后以单并发重新构建，287.5 秒明确通过。沙箱内全量测试唯一失败为既有 `file_association_integration` 注册表访问权限；同一构建产物在获准环境定向复核通过，不记为产品缺陷。

## 4. 8.2 验收记录

### 实现范围

- 新增 `DocumentSplitService::Prepare/Commit` 和拥有型 `SplitPlan`；服务只读取固定 `SessionSnapshot`，不调用会话提交、不修改脏状态或撤销历史。
- 切分点再次验证 UTF-8 码点和 CRLF 边界；Markdown 前后候选分别预解析，并保留“风险需确认”门禁。
- 当前 `.md` 只允许生成 `.md`，`.markdown` 只允许生成 `.markdown`，`.txt` 只允许生成 `.txt`；两个目标必须不同、不等于源路径且预先不存在。
- 两个结果继承当前编码和换行。实现先独占写入、刷新并逐字节校验两个同目录临时文件，再以不覆盖移动提交。
- 第二目标出现竞争或提交失败时删除本次已提交的第一目标和临时文件；竞争者文件不覆盖、不删除。补偿失败返回独立严重错误，绝不报告完整成功。
- 新增 4101～4105 稳定错误码并同步错误码表。

### 自动证据

| 场景 | 结果 |
| --- | --- |
| Markdown 切分、UTF-16 LE 与 CRLF 继承 | 通过 |
| TXT 纯文本切分、LF 与扩展名继承 | 通过 |
| UTF-8 码点中间拒绝 | 通过 |
| 错误扩展名/既有目标冲突拒绝 | 通过 |
| 第二目标竞争时第一目标补偿删除、竞争文件保留 | 通过 |
| 原会话源码、脏状态和源文件不变 | 通过 |
| 文首切分产生合法空前段 | 通过 |
| `document_split_unit` | 通过 |
| Debug 正式应用和新增测试目标构建 | 通过 |
| 全量 Debug CTest | 39/39 通过；注册表项在可访问隔离 HKCU 环境复核通过 |
| Release 核心库与正式 `MarkdownMay.exe` | 通过 |

完整 Debug `ALL_BUILD` 三次被 360 秒工具外部时限截断且均无编译诊断；清理遗留进程后，正式 Debug 应用及新增测试目标分别构建通过，并以全部 39 项测试验证所有测试可执行产物。Release 预设两次同样超时后，直接对同一生成目录的 Release 配置并行构建核心库和正式应用，117.3 秒明确通过。上述超时不记录为构建通过，也不判为源代码失败。

## 5. 8.3 验收记录

### 实现范围

- 新增 `DocumentCombineService::Prepare/Commit`、`InsertPlan` 和 `InsertResult`；来源只读加载为固定快照，来源文件不移动、不覆盖、不删除。
- `.md/.markdown/.txt` 复用既有 UTF-8 BOM、UTF-16 LE/BE BOM、严格 UTF-8 解码，静默转为会话内部 UTF-8；插入后不保留独立来源编码。
- 插入只补足前后边界所需的最少换行，并通过一次 `DocumentSession::commit` 提交，产生一个应用级事务和一个新修订。
- Markdown 普通相对链接按来源目录解析并以纯词法方式改写为相对当前文档的新路径；目标文件不必预先存在。跨盘符等无法安全改写的引用明确失败。
- Markdown 本地图片复制到当前 `<文档名>.assets`，使用不冲突名称并改写引用；远程图片、锚点和绝对 URI 原样保留。代码围栏和行内代码中的伪链接不改写。
- 当前文档未保存且来源 Markdown 含需改写的相对引用时拒绝执行。TXT 来源不扫描链接或图片。
- TXT 插入纯文本会话时保持原字符；TXT 插入 Markdown 会话时转义会触发 Markdown 语义的行首/行内标记，确保 `#` 等仍作为可见普通字符。
- 资源先复制、正文后提交；正文提交失败时只删除本事务新建资源。成功结果返回新建资源清单，供 UI 层将其登记到同一撤销项。
- 新增 4201～4207 稳定错误码并同步错误码表。

### 自动证据

| 场景 | 结果 |
| --- | --- |
| UTF-16 BE TXT 静默解码并插入纯文本会话 | 通过 |
| TXT 的 `#` 在纯文本/Markdown 目标中均保持普通字符语义 | 通过 |
| Markdown 普通相对链接词法重写 | 通过 |
| 本地图片复制、目标改写和内容校验 | 通过 |
| 远程图片保留且不下载 | 通过 |
| 代码围栏内伪链接不改写 | 通过 |
| 未保存目标与不支持扩展名拒绝 | 通过 |
| 正文提交失败后新建图片回滚、正文不变 | 通过 |
| `document_combine_unit` | 通过 |
| Debug 正式应用与插入测试目标构建 | 通过 |
| 全量 Debug CTest | 40/40 通过；注册表项在可访问隔离 HKCU 环境复核通过 |
| Release 核心库与正式 `MarkdownMay.exe` | 通过 |

定向测试首次暴露图片语法识别位置错误，修正为从目标反查标签起点；全量测试随后暴露 CTest 工作目录和不存在普通链接目标导致的路径差异，最终统一规范化当前文档绝对路径，并使用同盘符校验后的 `lexically_relative`，使重写不依赖目标实际存在。以上均在提交前通过回归。

## 6. 8.3B 风险样机记录

完整证据见 `第八阶段8.3B_DOCX与PDF文字插入风险验证报告.md`。

- DOCX：真实 Deflate 压缩包的 H1/H2、中文正文、项目符号和表格结构提取通过；但用户已根据样机决定放弃 DOCX 插入，不引入 `miniz`。
- PDF：文字层中文提取、无文字层零字符和加密拒绝样机通过；但完整实现必须使用 PDFium 等成熟引擎。开发运行时 PDFium DLL 约 6.88 MB，当前 Release EXE 约 1.84 MB，且官方构建/依赖复杂，当前不满足轻量单 EXE门禁。
- 决策：本增量没有把 Python、Poppler、PDFium DLL 或 Office 写入产品代码；用户最终放弃 DOCX/PDF 插入，二者均不作为延期待办，正式插入范围固定为 `.md/.markdown/.txt`。
- DOCX 视觉渲染因本机没有 LibreOffice未执行，按技能规则降级为 ZIP/XML 结构检查；PDF 两个页面均渲染为 PNG并逐页目视检查，文字层页无缺字/乱码，无文字层页只有可见图形。

## 9. 8.7 验收记录

### 自动与真实进程证据

| 项目 | 结果 |
| --- | --- |
| Debug `ALL_BUILD` | 53.6 秒通过 |
| Debug 全量 CTest | 40/40 通过 |
| Release `ALL_BUILD` | 61.4 秒通过 |
| Release 全量 CTest | 40/40 通过 |
| Release 正式 EXE 五次启动 | 5/5 `WaitForInputIdle` 成功、均响应 |
| Release 窗口标题 | 5/5 为“无标题 - 马冬梅” |
| Release 启动观测 | 380.2～527.9 ms（含 250 ms 稳定等待） |
| Release 工作集 | 35.10～35.32 MB |
| Release EXE | 2.07 MB，单 EXE；新增体积主要来自三组多尺寸图标 |
| 真实用户文件关联 | 未改写；全程只使用隔离 HKCU 自动测试 |

### 尚需用户侧视觉复核

Windows 应用控制插件连续两次因本机 Codex 安装目录 `EPERM` 无法初始化，因此没有自动操作资源管理器或切换系统显示设置。以下项目不得标为已通过：

1. 资源管理器小/中/大/超大图标视图中的应用、Markdown/TXT 文档图标观感；
2. 100%、125%、150%、200% 显示缩放；
3. 浅色、深色和高对比度主题下的辨识度；
4. 用户亲自在 Windows 默认应用界面选择马冬梅后的 `.md/.txt` 双击体验。

这些是视觉与用户选择验证，不影响自动构建、数据安全或候选注册正确性，但按 V05 ICON-005 和 8.7 门禁，收到用户复核结论前不把第八阶段写成“最终封闭”。
