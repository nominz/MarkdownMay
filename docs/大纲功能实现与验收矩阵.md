# 大纲功能实现与验收矩阵

> 变更：CR-2026-002  
> 上位需求：`需求规格说明书_V03.md` 1.0  
> 日期：2026-08-09

| 增量 | 范围 | 代码/测试 | 状态 |
| ---: | --- | --- | --- |
| 1 | V03 冻结、影响与设计同步 | 需求、总体/详细设计、功能矩阵 | 已完成 |
| 2 | H1～H6 只读大纲投影 | `ui/outline_view.*` | Debug 集成通过 |
| 3 | 固定左侧布局与同字号缩进 | `ui/document_window.*`、UI 集成测试 | Debug 集成通过 |
| 4 | 菜单、状态栏双开关 | command/menu/status bar、外壳测试 | Debug 集成通过；视觉待用户复测 |
| 5 | 三模式节点定位 | `view_mode_controller.*`、集成测试 | 当前表面定位链路通过；三模式人工复测待完成 |
| 6 | INI V3 显示偏好 | local services 单元/集成测试 | Debug 单元测试通过 |
| 7 | Debug/Release 回归与人工视觉验收 | 全量 CTest、Release EXE | 自动验证完成；人工视觉待用户复测 |
| 8 | 2026-08-10 优化：按标题自动显隐、可调宽、水平滚动、状态栏顺序、对照回渲染重绘 | `document_window.*`、`outline_view.*`、`split_view.*`、`status_bar.*`、`view_mode_controller.cpp` | 相关 Debug 集成 3/3 通过；Release 主程序构建通过；人工视觉待复测 |

验收必须证明：默认显示；用户关闭后重启仍关闭；菜单和状态栏状态一致；隐藏时状态栏文字为灰色；H1～H6 仅以缩进区分；点击在三种模式中定位且不改变正文；既有功能无回归。

## 自动验证记录

- Debug 主程序与全部测试目标构建成功。
- `services_unit` 通过：V3 设置往返、V1/V2 迁移默认显示和 `outline_visible=false` 均通过。
- `app_shell_integration` 通过：默认左侧布局、H1/H3 同字号缩进文本、节点点击定位且不标脏、菜单显示切换均通过。
- 全量 CTest：36 项中 34 项通过。`startup_single_instance_integration` 因当前环境已有马冬梅实例占用互斥体而返回 6；`wysiwyg_block_integration` 为既有块编辑测试失败，连续复跑三次仍失败，两项均不在本次大纲改动文件链路中，不能记作通过。
- Release 主程序单进程构建成功，生成 `build/windows-x64-debug/app/Release/MarkdownMay.exe`（1,651,200 字节）。
