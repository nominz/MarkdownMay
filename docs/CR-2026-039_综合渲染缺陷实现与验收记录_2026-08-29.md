# CR-2026-039 综合渲染缺陷实现与验收记录

> 日期：2026-08-29
>
> 对应缺陷：DEF-051～058
>
> 需求基线：`需求规格说明书_V07.md` 1.9

## 1. 影响分析与实现

- 引用线改为保留槽固定横坐标及统一垂直校正，消除子块几何差异导致的原生表格重叠。
- 标题类型标志和手柄按 TOM 实际字形行框中心布局；列表增加 240 twips 基础缩进，自动标记记录保护范围，有序编号提供常用起始值菜单。
- 表格通知门禁增加整份 table-aware 可见文本比较，修复表格外回车被误判为格式变化。
- 分隔线扩展为 14 个线段字符；行内代码改为淡粉背景和正文减 1 pt。

## 2. 自动验收

| 项目 | 结果 |
| --- | --- |
| Debug 全构建 | 已通过（首次重建后行内测试缺少 `commdlg.h`，补齐后目标重编通过） |
| Debug 定向 `wysiwyg_inline/block/list/input` | 4/4 通过；最终圆角覆盖层加入后 `wysiwyg_inline` 再次通过 |
| Debug 全量 | 44/47；`heading_fold_integration`、`block_interaction_integration` 和受限环境既有 `file_association_integration` 失败。前两项均在本轮变更前交接中已有非确定性失败记录；`block_interaction` 本轮直接读取为既有末段 add-below/undo 断言退出码 43，不是标题居中早期断言。未写成全量通过 |
| Release 全构建 | 已通过；最终圆角覆盖层加入后 Release 主程序与 `wysiwyg_inline` 目标重编成功 |
| Release 定向 | `wysiwyg_inline/block/list/input` 4/4 通过；`block_interaction_integration` 仍为既有非确定性失败，未冒充通过 |

## 3. 人工复核

需用用户指定长文档复核引用含表格、短/多行引用、H2 控件、列表编号菜单、任意段尾回车、分隔线和四种主题下行内代码视觉。圆角外框已由主题感知 GDI 空心圆角轮廓实现，但仍须人工像素验收，不以颜色测试替代观感确认。
