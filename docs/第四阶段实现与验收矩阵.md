# 马冬梅第四阶段实现与验收矩阵

> 文档编号：MM-DEV-004  
> 状态：执行中  
> 路线：瀑布开发、依赖有序增量、阶段内不发布

## 阶段交付物

不依赖 GUI 的 `markdownmay_core` 静态库，以及完整单元、集成和往返测试。

## 实施顺序

| 顺序 | 增量 | 正式目录 | 封闭测试 | 状态 |
| ---: | --- | --- | --- | --- |
| 1 | 编码、换行、路径、原子读写 | `src/fileio` | `tests/unit/fileio` | 已封闭 |
| 2 | 解析、节点、索引、校验 | `src/markdown`、`src/document` | `tests/unit/markdown`、`document` | 已封闭 |
| 3 | 序列化、未知块保留 | `src/markdown` | `tests/roundtrip` | 已封闭 |
| 4 | 编辑事务和事件 | `src/document` | `tests/unit/document` | 已封闭 |
| 5 | 设置、日志、恢复、最近文件 | `src/services` | `tests/unit/services`、`integration` | 执行中 |
| 6 | 图片存储与路径 | `src/fileio` | `tests/unit/image`、`integration` | 未开始 |

## 阶段出口

固定样例必须完成“打开 → 解析 → 修改 → 保存 → 重开”，语义比较全部通过；核心库依赖表不得出现 GUI、RichEdit 或 Scintilla。
