# 第七阶段实现与验收矩阵

> 上位需求：`需求规格说明书_V04.md` 1.0
>
> 启动基线：`1663d36 docs: freeze V04 requirements baseline`
>
> 状态：进行中

| 增量 | 范围 | 主要实现/测试 | 状态 |
| ---: | --- | --- | --- |
| 7.0 | 准入基线和既有撤销缺陷收口 | `editor/richedit_host.cpp`、36 项回归 | 已完成并提交 `730619c` |
| 7.1 | 不可变导出中间模型 | `src/export/export_document.cpp`、`tests/unit/export/export_document_test.cpp` | 已实现并自动验证 |
| 7.2 | HTML 单文件/公式系统字体风险样机 | `prototype/core/safe_html_probe.*`、`tests/html_export_probe.cpp`、7.2 风险报告 | 已实现并验证；Mermaid 正式绘图受依赖审查门禁 |
| 7.3 | 通用文件、任务、进度、取消和原子提交 | 待实现 | 未开始 |
| 7.4 | 仅大纲/全文 TXT | 待实现 | 未开始 |
| 7.5 | 仅大纲/全文 PDF | 待实现 | 未开始 |
| 7.6 | 仅大纲/全文 DOCX | 待实现 | 未开始 |
| 7.7 | 全文 HTML | 待实现 | 未开始 |
| 7.8 | UI、兼容性与阶段封闭 | 待实现 | 未开始 |

## 7.1 验收记录

### 实现范围

- `ExportScope` 冻结为 `outline/full`，`ExportFormat` 冻结为 `pdf/docx/txt/html`。
- 组合门禁拒绝 `outline × html`，允许 V04 的其余七种组合。
- `BuildExportDocument` 只接收一次 `SessionSnapshot` 和期望修订。
- 创建前验证语义存在、源码/解析/期望/语义修订一致且文档快速不变量通过。
- `outline` 递归筛选 H1～H6，保持源顺序、真实层级、NodeId 和源范围。
- `full` 复制根节点下全部块及其行内/子结构。
- 导出节点使用拥有型值和递归容器，不持有 `Document`、`Node*` 或任何 UI 对象。

### 自动证据

| 项目 | 结果 |
| --- | --- |
| CMake 配置 | 通过 |
| `export_document_unit` | 通过 |
| 完整 Debug 构建 | 通过 |
| 全量 Debug CTest | 37/37 通过，总耗时 3.44 秒 |
| Release `MarkdownMay` 构建 | 通过 |

测试覆盖合法/非法 scope-format 组合、仅大纲层级和顺序、全文块复制、期望修订不一致、解析修订落后和语义缺失。各 writer、文件任务、进度取消、原子提交和格式兼容性不属于 7.1，不能据此声明导出文件已经可用。

完整 Debug 构建首次超过 120 秒工具时限，强制中止遗留的编译进程造成一次 PDB C1041；待残留进程退出后单目标及完整构建均通过。该事件属于构建进程被外部时限中止，不是源代码编译缺陷。

## 7.2 验收记录

- 单文件 HTML 样机不包含伴随资源、脚本节点或外部资源属性；结构测试通过。
- 严格 CSP 禁止默认资源、脚本、连接、字体、对象、base 和表单提交。
- 攻击标签只作为可见文字保留，真实浏览器 DOM 未出现攻击脚本/图片节点或执行标记。
- Mermaid 当前显示明确源码回退；浏览器无原生 Mermaid 能力，固定渲染资源必须先通过许可证、安全、CSP、体积和单 EXE 审查。
- MathML 在当前浏览器使用 Windows `Cambria Math` 完成分式、根式和上标二维排版，基础系统字体路线通过风险验证。
- 浏览器安全策略禁止自动化直接打开 `file://`；本轮通过仅监听 `127.0.0.1` 的临时只读服务验证同一构建产物，结束后服务已停止。真实双击文件进入后续人工兼容性测试。

完整证据见 `第七阶段7.2_HTML安全与公式系统字体风险验证报告.md`。本增量没有实现正式 HTML writer、LaTeX 转 MathML 解析器或 Mermaid 绘图，不得据此声明用户导出功能可用。
