# CR-2026-032 工具栏治理实现与验收矩阵

> 日期：2026-08-23
>
> 需求基线：`需求规格说明书_V07.md` 1.2，TOOLBAR-001～008

## 1. 影响结论

- 修改 UI、命令路由、Win32 资源、测试及第三方说明；不修改 `DocumentSession`、Markdown 解析器、文件格式、快捷键和文档模型。
- CR-2026-022 的逐图标字体/GDI 自绘方案被集中 SVG 渲染层替换。
- 代码块和水平分隔线复用已有编辑器能力；打开、保存、打印、查找、替换、模式、大纲和渲染风格复用既有命令。
- 标准 Markdown 无下划线语义，本变更不引入 HTML `<u>` 扩展，保留既有删除线入口。

## 2. 自动验收

- [x] 正式 SVG 以 RCDATA 嵌入，不使用绝对路径。
- [x] 集中路径解析、动态颜色、18 px 图标盒和 DPI 缩放。
- [x] Checked 图标本体使用 `#159CB5`，无蓝点、底线或整块蓝底。
- [x] 新增打开、保存、打印、代码块、水平分隔线、替换和大纲入口。
- [x] 三模式继续使用 `BTNS_CHECKGROUP`，大纲独立使用 `BTNS_CHECK`。
- [x] 表格、渲染风格绘制 8 px chevron。
- [x] 段落菜单使用 H1～H6 递减字号、字重和行高。
- [x] Tabler 官方许可证、来源和感谢文件齐备。
- [x] Debug/Release `ALL_BUILD` 成功；两配置全量各 46/47，唯一失败均为受限沙箱不能写 HKCU 的既有文件关联项，沙箱外隔离复跑均通过，故两配置最终均为 47/47。

## 3. 人工复核

- [x] 100% DPI Release 实窗：图标完整、居中、无裁切，分组和青蓝 Active 状态符合施工说明。
- [ ] 125%、150% DPI 下图标完整、居中、无裁切。
- [ ] 浅色、深色、高对比度下 Normal/Hover/Pressed/Checked/Disabled 可辨。
- [x] 100% DPI 下表格与渲染风格 chevron 轻量紧凑；段落弹层 H1～H6 递减层级、留白和当前项细青蓝提示符合预期。
- [ ] 窄窗口下工具栏可用性和所有中文 ToolTip。

## 4. DEF-041 工具栏行为回归（2026-08-23）

- [x] 粗体无选区静默无动作；有选区连续点击可添加和移除，青蓝图标状态同步。
- [x] 代码块、水平分隔线命令通过工具栏可达；格式菜单包含代码块，插入菜单包含分隔线。
- [x] 引用、无序列表、有序列表、任务列表按当前块返回 Checked 状态；渲染风格弹出按钮始终不返回 Checked。
- [x] 引用块绘制主题感知灰色竖向导引线。
- [x] 无序、有序、任务列表使用悬挂缩进，换行与正文起始列对齐。
- [x] 2026-08-24 DEF-041 复发：RichEdit `dxOffset` 已由负值改为正值，回归断言同步要求“续行位置严格在首行标记右侧”；Debug/Release 定向列表测试各 1/1 通过，`ALL_BUILD` 两配置成功。全量回归 Debug 45/47 后输入项隔离复跑通过，Release 46/47；两配置唯一持续失败均为既有 50 KiB 投影性能门限（当前环境分别约 105 秒/69 秒），与本次常量方向修正无代码路径关系，不虚报全量通过。用户提供的两张截图仅作为视觉预期，不采纳其中正文内容。
- [x] Debug/Release `ALL_BUILD` 成功；两配置全量均完成 47 项，文件关联项因沙箱 HKCU 限制失败后在沙箱外隔离通过；Debug 输入项在 Debug/Release 并行 GUI 全量中偶发一次失败，隔离复跑通过。Release 实窗仍待用户视觉复核。
