# 马冬梅数据结构图

> 文档编号：MM-DATA-001
> 版本：1.0
> 对应需求基线：V02 1.0

## 1. 文档会话聚合

```mermaid
classDiagram
    class DocumentSession {
      +DocumentId id
      +FileMetadata file
      +SourceBuffer source
      +Revision sourceRevision
      +Revision parsedRevision
      +Revision savedRevision
      +SemanticDocument semantic
      +ParseState parseState
      +ViewMode mode
      +DirtyState dirty
      +ExternalState external
    }

    class FileMetadata {
      +Path path
      +TextEncoding encoding
      +LineEnding lineEnding
      +FileIdentity identity
      +bool readOnly
    }

    class SourceBuffer {
      +Utf8Text text
      +LineIndex lines
    }

    class SemanticDocument {
      +NodeId rootId
      +BlockNode[] blocks
      +NodeIndex index
    }

    class ViewStateSet {
      +RenderViewState render
      +SourceViewState source
      +SplitViewState split
    }

    class UndoCoordinator {
      +UndoEntry[] undo
      +UndoEntry[] redo
    }

    DocumentSession *-- FileMetadata
    DocumentSession *-- SourceBuffer
    DocumentSession o-- SemanticDocument
    DocumentSession *-- ViewStateSet
    DocumentSession *-- UndoCoordinator
```

## 2. 语义文档树

```mermaid
classDiagram
    class Node {
      <<abstract>>
      +NodeId id
      +NodeKind kind
      +SourceRange source
    }
    class BlockNode {
      <<abstract>>
    }
    class InlineNode {
      <<abstract>>
    }
    class DocumentRoot
    class Paragraph
    class Heading {
      +uint8 level
    }
    class Quote
    class List {
      +ListKind kind
      +uint32 start
    }
    class ListItem {
      +TaskState task
    }
    class CodeBlock {
      +string language
      +string text
    }
    class Table {
      +uint32 columns
      +Alignment[] alignment
    }
    class TableRow
    class TableCell
    class ThematicBreak
    class UnknownBlock {
      +bytes original
    }
    class TextRun {
      +string text
    }
    class Emphasis
    class Strong
    class Strike
    class InlineCode
    class Link {
      +string target
      +string title
    }
    class Image {
      +string target
      +string alt
      +ImageSize size
    }

    Node <|-- BlockNode
    Node <|-- InlineNode
    BlockNode <|-- DocumentRoot
    BlockNode <|-- Paragraph
    BlockNode <|-- Heading
    BlockNode <|-- Quote
    BlockNode <|-- List
    BlockNode <|-- ListItem
    BlockNode <|-- CodeBlock
    BlockNode <|-- Table
    BlockNode <|-- TableRow
    BlockNode <|-- TableCell
    BlockNode <|-- ThematicBreak
    BlockNode <|-- UnknownBlock
    InlineNode <|-- TextRun
    InlineNode <|-- Emphasis
    InlineNode <|-- Strong
    InlineNode <|-- Strike
    InlineNode <|-- InlineCode
    InlineNode <|-- Link
    InlineNode <|-- Image
    DocumentRoot "1" *-- "*" BlockNode
    BlockNode "1" *-- "*" Node
```

## 3. 版本与投影关系

```mermaid
flowchart LR
    DISK["磁盘字节\nSavedRevision"]
    SRC["SourceBuffer\nSourceRevision"]
    SEM["SemanticDocument\nParsedRevision"]
    RICH["RichEdit 投影\nRenderRevision"]
    SCI["Scintilla 文本\nSourceViewRevision"]
    SPLIT["对照右栏\nSplitRenderRevision"]
    EXPORT["ExportDocument\nExportRevision"]

    DISK -->|"解码"| SRC
    SRC -->|"MD4C 解析成功"| SEM
    SEM -->|"投影"| RICH
    SRC -->|"显示/编辑"| SCI
    SEM -->|"只读投影"| SPLIT
    SEM -->|"快照"| EXPORT
    RICH -->|"编辑事务 + 序列化"| SRC
    SRC -->|"原子保存"| DISK
```

版本不变量：

```text
ParsedRevision <= SourceRevision
SavedRevision <= SourceRevision
RenderRevision = ParsedRevision
SplitRenderRevision = ParsedRevision
ExportRevision = SourceRevision 仅当 ParsedRevision = SourceRevision
```

## 4. 编辑事务

```mermaid
classDiagram
    class EditTransaction {
      +TransactionId id
      +Revision before
      +Revision after
      +EditOrigin origin
      +ModelOperation[] modelOps
      +FileOperation[] fileOps
      +SelectionSnapshot beforeSelection
      +SelectionSnapshot afterSelection
    }
    class ModelOperation {
      <<variant>>
    }
    class InsertText
    class DeleteRange
    class ReplaceNode
    class SetAttribute
    class MoveNode
    class FileOperation {
      <<variant>>
    }
    class RenameManagedImage {
      +Path from
      +Path to
    }
    class SelectionSnapshot {
      +NodePosition anchor
      +NodePosition caret
    }

    EditTransaction *-- ModelOperation
    EditTransaction *-- FileOperation
    EditTransaction *-- SelectionSnapshot
    ModelOperation <|-- InsertText
    ModelOperation <|-- DeleteRange
    ModelOperation <|-- ReplaceNode
    ModelOperation <|-- SetAttribute
    ModelOperation <|-- MoveNode
    FileOperation <|-- RenameManagedImage
```

## 5. 图片数据

```mermaid
classDiagram
    class ImageReference {
      +NodeId node
      +string markdownTarget
      +ImageLocationKind kind
      +Path resolvedPath
      +bool managed
      +ImageStatus status
    }
    class ManagedImageRename {
      +Path originalPath
      +Path markedPath
      +bool completed
    }
    class ImageCacheKey {
      +FileIdentity identity
      +uint32 width
      +uint32 height
      +Theme theme
    }
    class DecodedImage {
      +uint32 width
      +uint32 height
      +BitmapHandle bitmap
    }

    ImageReference o-- ManagedImageRename
    ImageCacheKey --> DecodedImage
```

## 6. 错误对象

```mermaid
classDiagram
    class Error {
      +ErrorCode code
      +Severity severity
      +Subsystem subsystem
      +uint32 systemCode
      +string detailKey
      +SourceLocation source
    }
    class Status {
      +bool ok
      +Error error
    }
    class Result~T~ {
      +bool ok
      +T value
      +Error error
    }
    Status o-- Error
    Result o-- Error
```

## 7. 枚举基线

```text
ViewMode        = Render | Source | Split
ParseState      = Empty | Parsing | Valid | Invalid
DirtyState      = Clean | Modified | RecoveryOnly
ExternalState   = Unchanged | Changed | Deleted | Replaced
TextEncoding    = Utf8 | Utf8Bom | Utf16Le | Utf16Be
LineEnding      = CrLf | Lf | Mixed
ImageLocation   = Managed | ExternalLocal | Remote | Missing
TaskState       = None | Unchecked | Checked
Severity        = Info | Warning | Error | Fatal
```
