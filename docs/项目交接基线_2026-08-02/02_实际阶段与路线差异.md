# 实际阶段、增量与路线差异

> **2026-08-12 路线变更：** 第七阶段导出已封闭；新增第八阶段“文档组合、TXT 文档支持、Windows 文件关联与文档图标”。原完整系统测试顺延为第九阶段，原发布准备与最终验收顺延为第十阶段。详见 `../第八阶段新任务接续交接_2026-08-12.md`。

> **2026-08-22 路线变更：** 新增第九阶段“块悬停与段落手柄”；完整系统测试顺延为第十阶段，发布准备与最终验收顺延为第十一阶段。详见 `../需求规格说明书_V07.md`。

## 1. 当前总进度

| 阶段 | 原计划目标 | 实际状态 | 实际交付/验收入口 |
| ---: | --- | --- | --- |
| 1 | 需求冻结 | 已完成 | `需求规格说明书_V01.md`、`V02.md`、变更影响矩阵 |
| 2 | 总体和详细设计 | 已完成 | 总体设计、详细设计、接口草案、数据结构图、错误码表 |
| 3 | 工程基础与风险样机 | 已完成 | `第三阶段风险验证报告.md`、`prototype` |
| 4 | 底层库实现 | 已完成，7 个实际提交增量 | `第四阶段实现与验收矩阵.md`、底层库实现报告 |
| 5 | 所见即所得编辑器 | 已完成，13 个增量 | `第五阶段实现与验收矩阵.md`、编辑器实现报告 |
| 6 | 应用外壳与系统集成 | 已完成，9 个增量 | `第六阶段实现与验收矩阵.md` |
| 7 | PDF/DOCX/TXT/HTML 导出 | 已完成并封闭 | `第七阶段实现与验收矩阵.md`、导出兼容性报告 |
| 8 | 文档组合、TXT 与 Windows 文件类型集成 | 已按用户粗验接受范围封闭；完整兼容性人工矩阵转第十阶段 | `第八阶段实现与验收矩阵.md` |
| 9 | 块悬停与段落手柄 | 9.1～9.6 已实现并完成自动验收；最终人工兼容性复核待用户执行 | `需求规格说明书_V07.md`、第九阶段专用矩阵 |
| 10 | 完整系统测试 | 未开始 | 待第九阶段封闭；含真实设备和性能/容量矩阵 |
| 11 | 发布准备与最终验收 | 未开始 | 安全、隐私、兼容性、发布物和逐项需求验收 |

## 2. 实际增量拆分

### 第四阶段：底层库

实际按依赖拆为：文件底层、文档模型与解析、Markdown 序列化、文档事务、本地服务、图片存储、总验收。对应提交从 `5c44dd2` 到 `0b9d5ae`。

### 第五阶段：编辑器

实际 13 个增量：

1. 纯段落编辑；
2. 行内格式；
3. 标题、引用、代码块、分隔线；
4. 列表；
5. 图片编辑；
6. 受管图片事务；
7. 表格；
8. 输入与查找；
9. 完整投影；
10. 源码模式；
11. 对照模式；
12. 三模式控制；
13. 保存重开；
14. 随后另有正式收口提交 `478603c`，不作为新增产品增量计数。

详细结论以 `第五阶段实现与验收矩阵.md` 为准。

### 第六阶段：应用外壳

实际 9 个增量：

1. 应用入口、生命周期、主窗口和文档容器；
2. 稳定命令编号、命令分发、中文菜单和快捷键；
3. 工具栏、三模式区和状态栏；
4. 新建、打开、保存、另存为和拖放；
5. 最近文件、未保存提示、只读和外部修改；
6. 命令行、单实例和文件队列；
7. 文件关联和默认应用引导；
8. 主题、高对比度、DPI 和多显示器；
9. 打印、本地设置和应用程序集成总验收。

对应提交从 `d7eecdc` 到 `155d9c4`，详细结论以 `第六阶段实现与验收矩阵.md` 为准。

## 3. 与原始路线的差异

- 九个总阶段没有被敏捷迭代替换，仍遵守瀑布顺序。
- 第四至第六阶段内部的“增量”是依赖有序的实现与验证批次，不是对外发布的小版本。
- 实际增量比最早技术方案更细，因为风险样机和实现中发现文件状态、受管图片、三模式同步、单实例、文件关联、主题等需要独立封闭。
- 这些细分没有改变总体架构：文档模型先于编辑器，编辑器先于应用外壳，导出后置。
- 第六阶段完成后出现新的“大纲功能”需求。按变更控制，它不是偷偷追加到已封闭第六阶段，也不应等到第九阶段才处理；计划新增一个“第六阶段后需求变更基线/大纲增量”，完成后再进入第七阶段。

## 4. 下一条受控路线

1. 《需求规格说明书_V03》大纲部分已按用户明确交互冻结。
2. 功能矩阵、总体设计、详细设计和大纲验收矩阵已同步。
3. 固定左侧窗格、双入口开关、状态栏状态、标题导航和 INI V3 已实现。
4. Debug 大纲相关测试及 Release 主程序构建已通过；全量 36 项中 34 项通过，两项既有/环境失败已记录。
5. 完成人工视觉复测并处理仍可复现的非大纲回归后形成最终封闭证据。
6. 更新本交接目录，使其指向新的代码基线。
6. 大纲增量最终封闭后恢复第七阶段 PDF/DOCX 导出。

新任务不得因为本文件给出了路线，就跳过用户对 V03 的评审。
