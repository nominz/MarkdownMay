# 第九阶段块悬停与段落手柄实现验收矩阵

> 建立日期：2026-08-22
>
> 需求基线：`需求规格说明书_V07.md` 1.0
>
> 状态：需求与设计基线已建立；尚未开始功能实现

## 1. 阶段边界

本阶段实现 BLOCKUI-001～020：渲染块悬停标志、段落手柄、原生上下文菜单及其结构安全命令。它不增加拖拽排序，不改变 Markdown 文件格式，不为 TXT 构造 Markdown 块，不建立第二套正文模型。

阶段输入是第八阶段及已批准受控增量的稳定基线。阶段出口是该交互通过自动与人工验收；随后才能进入第十阶段完整系统测试。

## 2. 依赖有序增量

| 增量 | 内容 | 主要证据 | 状态 |
| ---: | --- | --- | --- |
| 9.0 | V07、影响分析、总体/详细设计和矩阵冻结 | 文档评审；无代码提前实现 | 已完成 |
| 9.1 | 可见块索引、NodeId/Revision/range 门禁和布局命中 | 标题/段落/复杂块、过期修订、10,000 块测试 | 未开始 |
| 9.2 | DPI/主题感知的标题类型标志与段落手柄 | H1～H6、普通段落、折叠三角并存、滚动重定位 | 未开始 |
| 9.3 | 原生菜单、命令查询、键盘入口和可访问对象 | 打开/关闭/焦点/禁用态/UIA 人工与自动证据 | 未开始 |
| 9.4 | 转换、删除、复制、剪切和缩进事务 | 完整块范围、单步撤销、剪贴板、结构边界测试 | 未开始 |
| 9.5 | 在下方添加与三模式同步 | 渲染/对照同步、源码模式不叠加、TXT 禁用 | 未开始 |
| 9.6 | 性能、主题/DPI/无障碍与阶段封闭 | Debug/Release、全量回归、50 KB/10,000 块、人工矩阵 | 未开始 |

## 3. 门禁与风险

- 9.1 未证明稳定块身份和过期修订拒绝前，不接入任何修改正文的菜单命令。
- 所有修改命令必须复用 `DocumentSession` 事务和既有格式/剪贴板能力；禁止直接改 RichEdit/Scintilla 缓冲后再猜测同步。
- 标题折叠三角、悬停类型标志和手柄必须有互不重叠的命中区；高 DPI 下按逻辑单位重排。
- 表格、列表、引用、代码块的删除/缩进/添加若尚无安全结构规则，应禁用对应菜单项，不得按可见行粗暴截断源码。
- 未实际执行的构建、性能、屏幕阅读器、主题或 DPI 项不得写成“已通过”。

## 4. 阶段出口

BLOCKUI-001～020 全部具备可追溯证据；Debug/Release 构建与全量测试通过；用户完成悬停观感、菜单可用性、主题/DPI 和无障碍人工复核；所有阻断缺陷关闭后，形成本地 Git 封闭提交。默认不推送远端。
