# 查找替换功能实现与验收矩阵

> 变更：CR-2026-005  
> 需求：FIND-001～006  
> 设计：DC-2026-003  
> 日期：2026-08-10

## 1. 影响分析结论

本增量正式接入第五阶段已有的查找替换核心，并扩展通配符和特殊格式。功能继续以 `DocumentSession` 为唯一正文状态：查找只改变当前编辑表面的选区与滚动，替换通过既有编辑事务修改会话。Markdown 解析、序列化、文件编码/换行保存、图片、打印和未来导出没有结构变更；不增加第三方依赖、网络或运行时下载。

## 2. 需求—实现—测试

| 需求 | 实现 | 自动化证据 | 状态 |
| --- | --- | --- | --- |
| FIND-001 Ctrl+F/Ctrl+H | `ui/menu_controller.cpp`、`app/command_dispatcher.cpp`、`ui/find_replace_bar.*` | `app_shell_integration`、Debug/Release 主程序构建 | 通过 |
| FIND-002 三模式同一会话 | `editor/view_mode_controller.*` | `view_modes_integration`、`wysiwyg_input_integration` | 通过 |
| FIND-003 `?`、`*`、全角 `？` | `editor/find_replace_controller.*` | `input_controller_unit` | 通过 |
| FIND-004 `^p`、`^t` | `editor/find_replace_controller.*`、查找栏特殊格式菜单 | `input_controller_unit` | 通过 |
| FIND-005 非重叠替换和撤销 | `FindReplaceController::replace_all`、应用级会话历史 | `input_controller_unit`、既有撤销回归 | 通过 |
| FIND-006 大小写、空查询、未找到 | 查找栏选项和控制器失败路径 | `input_controller_unit` | 通过 |

## 3. 验证记录

- Debug 主程序目标：构建通过。
- Debug 定向 `input_controller_unit`：通过。
- Debug 全量：34/36；`wysiwyg_block_integration` 与 `file_association_integration` 失败，均为本变更前交接基线已记录的非查找替换问题；其余查找、输入、三模式和应用外壳测试通过。
- Release 主程序目标：构建通过。
- 人工待验：100%～300% DPI 下查找栏窄窗口布局、键盘焦点顺序、高对比度颜色，以及实际业务文档中的长模式性能。

## 4. 语义冻结

- 只有英文半角 `?` 和 `*` 在勾选“使用通配符”时具有通配符语义；中文 `？` 永远是普通文字。
- `?` 匹配一个 Unicode 字符；`*` 贪婪匹配同一段内零个或多个字符，不跨 CR/LF。
- `^p` 匹配 CRLF、LF 或 CR；替换中的 `^p` 写入内部 LF，文件保存层再应用目标换行策略。
- `^t` 匹配或插入 U+0009，即键盘 Tab 产生的制表符。
- 零长度匹配不参与查找和全部替换，避免仅含 `*` 的模式无限循环。
