# 第八阶段新任务接续交接

> 交接日期：2026-08-12
>
> 当前分支：`main`
>
> 当前基线：第七阶段导出已封闭；最新提交以新任务启动时 `git log -1` 为准
>
> 下一任务性质：正式需求变更与设计同步；不得直接实现代码

## 1. 当前结论

第七阶段导出已经实现并由用户验收通过。本任务到此结束。PDF 存在用户接受的非阻断版式瑕疵，留到最终大收尾统一精修；Word 导出获用户评价“相当完美”，个别调整属于特殊排版要求，不判为缺陷。

原路线必须重排：新增“第八阶段：文档组合、TXT 文档支持、Windows 文件关联与文档图标”；原“完整系统测试”顺延为第九阶段，原“发布准备与最终验收”顺延为第十阶段。此前创建的《第八阶段系统测试基线与实施矩阵》已改名并改口径为第九阶段草案，第九阶段不得在新第八阶段封闭前启动。

## 2. 用户已经明确的第八阶段范围

### 2.1 在光标插入点切分

- 三种编辑模式都必须把当前光标映射到 `DocumentSession` 唯一 UTF-8 源偏移。
- 把当前文档在该位置切分为两个新文件；当前原文件保持不变，不删除、不覆盖。
- 两个目标必须不同，全部写入并校验成功后才报告成功；部分成功不得冒充完整成功。
- 用户明确决定：切分生成了外部文件，因此切分操作不进入正文撤销栈，也不提供“撤销切分”。失败时仍须清理临时文件并保持原文不受损。

### 2.2 在光标插入点插入文件

- 选择另一个文件，把其内容插入当前光标位置；来源文件保持原样，不移动、不改写、不删除。
- 插入改变当前 `DocumentSession` 正文，应作为一次可撤销编辑事务。
- 编码、换行、UTF-8/CRLF 边界、结构粘连、相对链接和本地图片事务继续遵守 V04 COMBINE-005～010；具体交互与失败提示须在 V05 冻结。

### 2.3 `.md` / `.txt` Windows 集成

- 马冬梅同时注册为 `.md` 和 `.txt` 的候选打开程序，并提供应用内界面管理注册、撤销和跳转 Windows 默认应用设置。
- 不篡改 `UserChoice`，不逆向或伪造 Hash，不静默抢占默认程序。Windows 10/11 的最终默认选择由用户在系统界面确认。
- 用户目标是可以主动把马冬梅设为 `.txt` 默认程序，从而替代记事本；产品界面必须清楚说明“注册候选”和“在 Windows 中设为默认”的区别。
- `.txt` 打开后的精确编辑语义、Markdown 渲染边界、编码/换行保存策略尚需在新任务 V05 评审时确认，不得由实现自行猜测。

### 2.4 图标

- 保留现有应用图标 `resources/icons/MarkdownMay.ico`。
- 新增 Markdown 文档图标和 TXT 文档图标，Windows 文件类型最终使用多尺寸 `.ico`。
- 建议用户交付：`resources/icons/MarkdownMay.Document.md.ico`、`resources/icons/MarkdownMay.Document.txt.ico`；每个 ICO 尽量包含 16、20/24、32、40、48、64、128、256 像素的 32 位透明图层，另保留高清 PNG/SVG 源稿。
- 用户完成图标后告知路径；新任务负责嵌入 EXE 资源、ProgID `DefaultIcon`、资源管理器刷新和 DPI/主题人工验证。

## 3. 新第八阶段建议增量

| 增量 | 内容 | 门禁 |
| ---: | --- | --- |
| 8.0 | V05 需求、影响分析、总体/详细设计和阶段矩阵冻结 | 用户批准前不写功能代码 |
| 8.1 | 三模式光标到 UTF-8 源偏移、合法切分点与候选预解析 | 位置/编码/CRLF/结构测试 |
| 8.2 | 双目标原子切分、无撤销语义和失败恢复 | 原文件不变、双目标一致性 |
| 8.3 | 插入文件、单事务撤销、编码/换行和资源重写 | 来源文件不变、撤销闭环 |
| 8.4 | `.txt` 文档模型与打开/保存边界 | 待 V05 决策后细化 |
| 8.5 | `.md/.txt` 候选注册、撤销和默认应用引导界面 | 不写 `UserChoice` |
| 8.6 | 应用/MD/TXT 图标资源与 Shell 刷新 | 多尺寸和资源管理器验证 |
| 8.7 | Debug/Release、全量回归、人工验收和阶段封闭 | 完成后才能启动第九阶段 |

## 4. 新任务必须先做的事

1. 阅读根目录 `AGENTS.md`、本文件、`需求规格说明书_V04.md`、`功能模块与变更影响矩阵.md`、`总体设计说明书.md`、`详细设计说明书.md`。
2. 检查工作区并保留用户未跟踪文件；默认只做本地提交，不推送 GitHub。
3. 建立《需求规格说明书_V05》、第八阶段影响分析和实现验收矩阵；先交用户评审。
4. 重点解决 V04 与新决定的关系：切分不进撤销栈；插入仍是单次可撤销正文事务；`.txt` 是纯文本语义还是 Markdown 语义必须由用户冻结。
5. 用户批准 V05 后，严格按 8.1～8.7 依赖顺序实现。

## 5. 可复制的新任务开场提示

```text
这是马冬梅项目的新第八阶段任务。请先完整阅读根目录 AGENTS.md 和
docs/第八阶段新任务接续交接_2026-08-12.md，再按其中入口阅读 V04、
功能影响矩阵、总体设计和详细设计。第七阶段导出已经封闭；当前先做 V05、
影响分析和第八阶段设计冻结，不要立即写功能代码。新第八阶段包括光标处
切分为两个新文件（原文件不变、不可撤销）、光标处插入另一个文件（来源
文件不变、正文事务可撤销）、.md/.txt 候选注册及默认应用引导、应用/MD/TXT
三类图标。原系统测试和发布阶段顺延为第九、第十阶段。默认只做本地提交。
```
