# 马冬梅第五阶段实现与验收矩阵

> 文档编号：MM-DEV-005
> 状态：执行中
> 路线：瀑布开发、依赖有序增量、阶段内不发布

## 阶段边界

本阶段只实现覆盖完整需求范围的编辑器组件。主窗口、菜单、工具栏、文件关联和单实例属于第六阶段，不在本阶段提前实现。

## 实施顺序

| 顺序 | 增量 | 主要目录 | 封闭测试 | 状态 |
| ---: | --- | --- | --- | --- |
| 1 | 纯段落、光标、选择、输入、删除和撤销 | `editor/paragraph_editor.*`、`richedit_host.*` | `tests/unit/editor`、`tests/integration/wysiwyg_paragraph_*` | 已封闭 |
| 2 | 行内粗体、斜体、删除线、代码和链接 | `editor/inline_formatter.*`、`rich_projection.*` | 模型、投影、输入、往返 | 已封闭 |
| 3 | 标题、引用、代码块和分隔线 | `editor/block_formatter.*`、`rich_projection.*` | 模型、投影、输入、往返 | 已封闭 |
| 4 | 有序/无序/任务列表及嵌套缩进 | `editor/list_editor.*`、`rich_projection.*` | 模型、投影、输入、往返 | 已封闭 |
| 5 | 图片插入、显示、选中、缩放、替换和删除 | `editor/image_object.*`、`image_controller.*` | 模型、投影、输入、往返 | 已封闭 |
| 6 | 受管图片删除标记、重复引用和撤销恢复 | `editor/image_controller.*`、`undo_controller.*` | 图片复合事务 | 未开始 |
| 7 | 表格插入、导航、行列增删和粘贴 | `editor/table_editor.*` | 模型、投影、输入、往返 | 未开始 |
| 8 | 查找替换、剪贴板、拖放和格式命令 | `editor/*_controller.*` | 编辑操作矩阵 | 未开始 |
| 9 | 完整 Markdown 打开和渲染投影 | `editor/rich_projection.*` | 完整 fixtures | 未开始 |
| 10 | Scintilla 源码模式、错误定位和保存 | `editor/source_view.*`、`source_sync.*` | 源码模式集成测试 | 未开始 |
| 11 | 对照模式左侧编辑、右侧只读渲染和同步 | `editor/split_view.*` | 对照模式集成测试 | 未开始 |
| 12 | 三模式切换、位置恢复、跨模式撤销和版本一致 | `editor/view_mode_controller.*` | 三模式状态矩阵 | 未开始 |
| 13 | 保存重开一致性及异常回滚 | 横跨编辑器组件 | 总链路和故障注入 | 未开始 |

## 每个节点增量的封闭门槛

每增加一种节点，必须依次通过模型测试、投影测试、输入测试和 Markdown 往返测试，才能进入下一个增量。

增量 1 已建立应用级撤销栈并关闭 RichEdit 原生撤销；控件变更先计算单一最小差异，再作为带前置修订号的事务提交。无法映射为纯段落的变化会恢复最近合法投影，不修改会话版本。

增量 2 已实现粗体、斜体、删除线、行内代码和链接命令。RichEdit 投影隐藏 Markdown 标记及链接地址，以字符格式呈现语义，并维护可见 UTF-8 字节到源码字节的映射；粗体和链接内部连续输入、光标保持、撤销及往返测试均已覆盖。

增量 3 已实现一至六级标题、普通段落、引用、围栏代码块和分隔线命令。投影隐藏块标记，以字号、缩进、等宽背景和分隔线占位呈现语义；块内输入、撤销和往返均已覆盖。本增量同时修复 MD4C 生成软换行的源范围计算，并统一使用 RichEdit 原生字符位置换算跨段选区，避免 CRLF 累积偏移。

增量 4 已实现有序列表、无序列表、任务列表、勾选切换、四空格嵌套缩进和反缩进。投影使用项目符号、连续序号及勾选框呈现语义；支持项目正文输入、回车续建同类项目、任务续项自动清空勾选、空项目回车退出列表，并保持嵌套层级往返一致。

增量 5 已实现图片引用和本地文件插入、图片对象选中、10%～300% 显示比例、替换与引用删除。WIC 在解码前读取尺寸并限制一亿像素；本地可用图片、缺失/损坏图片及远程图片使用不同对象状态，远程地址只保留且绝不请求网络。文件插入复用 `<文档名>.assets` 安全复制规则；删除仅改变文档引用，受管文件的 `del_` 复合事务留待增量 6。

## 阶段出口

所有编辑操作矩阵通过；连续输入不发生模型/视图失步；崩溃和强制关闭后原文件不损坏。阶段结束前不发布中间版本。
