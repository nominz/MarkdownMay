# 马冬梅仓库协作入口

任何新的 Codex 任务在分析、设计或修改本仓库前，必须依次阅读：

1. `docs/项目交接基线_2026-08-02/00_新任务接续入口.md`
2. 该入口指定的、与本次工作有关的交接文档和正式基线文档

开发必须遵守以下长期规则：

- 采用瀑布开发和依赖有序增量；阶段或增量完成后形成验收证据和本地 Git 提交。
- 用户提出的新需求先做影响分析、升级需求基线并同步设计，不得直接绕过已封闭阶段修改代码。
- `DocumentSession` 是三种编辑模式唯一的文档状态，不得另建第二套正文模型。
- 保持 Windows 原生、轻量、无 WebEngine、无 Electron、无云服务和无运行时下载。
- 保留用户已有修改；默认只做本地提交，不主动推送 GitHub。
- 修改功能时同步检查 `docs/功能模块与变更影响矩阵.md` 指定的需求、设计、代码、测试和文档。
- 交付前至少执行与风险相称的 Debug/Release 构建和测试；不得把未验证结果写成“已通过”。

如果交接文档与更新后的正式需求或用户当前明确指令冲突，以用户当前指令和最新正式需求为准，并同步更新交接基线。
